import { createAction } from '@ngrx/store';

export const incrementX = createAction('[Counter Component] Increment X');
export const incrementO = createAction('[Counter Component] Increment 0');
export const resetGame = createAction('[Counter Component] Reset Game');


